import InputText from "../inputs/InputText";

export default function InputsGridForBooking(
  {children}
  :
  {children: React.ReactNode}
){
  return (
    <div className="inputs-grid-for-booking">
      {children}
    </div>
  )
}